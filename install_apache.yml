---

- hosts: all
  become: true
  tasks:

  - name: install apache2 php and libapache2-mod-php packages
    package:
      name: 
        - "{{ apache_package }}"
        - "{{ php }}"
        - "{{ php_package }}"
      state: latest
      update_cache: yes


